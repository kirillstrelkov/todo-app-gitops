apiVersion: batch/v1
kind: CronJob
metadata:
  name: create-random-task-cj
spec:
  schedule: "0 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: create-post-container
              image: CREATE_POST_RUN
              imagePullPolicy: IfNotPresent
              env:
                - name: URL
                  valueFrom:
                    configMapKeyRef:
                      key: POSTS_URL
                      name: todo-app-cm
          restartPolicy: OnFailure
